package com.prac.home.datastructures.trees;

/**
 * Question-> BSTSuccessorSearch->  is the node with smallest key greater than the key of the input node.
 * Logic will -> take the right one(greater one .. and then keep on searching left node with node)
 */
public class BSTSuccessorSearch {

    public static void main(String[] args) {

    }

    /*public Node successorSearch(Node data){

    }*/

    public Node searchDataNode(Node searchNode, Node rootNode){
        Node currentNode = rootNode;

        if(currentNode.data> searchNode.data){

        }
        return rootNode;
    }
}
