package com.prac.home.datastructures.array.sorting;

public class BubbleSort {
    // easy to understand
    // inefficient
    // check if let element is bigger than right then swap
    // first iteration few elements done
    // logic ...with every iteration largest element will be at the end
    // IF N ELEMENT ARE THERE OUTER LOOP(N-1)*(N-1)INNER LOOP  ITERATIONS YOU WILL APPLY IN ARAAY IF IT DESC ORDER
    // IN EFFICIENCY IS BECAUSE OF SWAPPING
    //

}
