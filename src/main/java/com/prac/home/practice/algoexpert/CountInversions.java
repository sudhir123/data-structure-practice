package com.prac.home.practice.algoexpert;

import java.util.Arrays;
import java.util.HashMap;
import java.util.Map;

/// bheryyy tough
public class CountInversions {
    //n^2 is very common .. n is i think not possible .. nlogn ...for that sorting is in picture /
    // 1 ways is sort and before that save number,idx in map.
    // another way  bst ...all right will have pair with left only condition is i<j.
    public static void main(String[] args) {
        CountInversions c= new CountInversions();
        System.out.println(c.countInversions(new int[]{2, 3, 3, 1, 9, 5, 6}));
    }

    public int countInversions(int[] array) {
        return -1;
    }

    // lets sort with merge sort for practice purpose
    public int[] mergeSort(int[] array){
       return null;
    }
}























